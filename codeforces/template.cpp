#include "bits/stdc++.h"
using namespace std;

void solve(){
    int N;
    cin >> N;
    // vector<int> arr(N);
    // for( auto& i : arr ){
    //     cin >> i;
    // }
}

int main(){
    int T;
    cin >> T;
    while(T--){
        solve();
    }
    return 0;
}